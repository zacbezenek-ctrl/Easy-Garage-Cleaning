# Easy-Garage-Cleaning

Employee signup and Hub access require production configuration in addition to a code deployment. Follow [Employee Hub recovery](docs/employee-hub-recovery.md) to preserve existing encrypted employee records, configure the owner and Firebase credentials, and verify recovery.

Run the automated checks with `node --test tests/*.test.mjs`.
